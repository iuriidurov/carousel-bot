# Telegram-бот "Carousel Bot"

Бот для автоматической генерации Instagram-каруселей, инфографики и постов для психологического блога с использованием AI.

## 🚀 Возможности

### 📊 Генерация каруселей
- Создание каруселей из 2-20 слайдов (формат 4:5)
- Динамический запрос изображения для первого слайда у пользователя
- Генерация контента через **Gemini-3-PRO** (Replicate API)
- Генерация изображений через **Nana Banana PRO** (Kie.ai API)
- Затемненный фон на первом слайде для лучшей читаемости текста
- Последовательная отправка слайдов по мере готовности

### 📈 Генерация инфографики
- **Режим 1:** После генерации карусели (опционально)
- **Режим 2:** Отдельный режим с генерацией структуры через Gemini
- Строгие требования к русскому языку
- Автоматическая отправка готовой инфографики

### 📝 Генерация постов
- **Режим 1:** Пост на основе карусели (после инфографики, опционально)
  - Использует структуру и контент из JSON карусели
  - Превращает тезисы в живой текст поста
- **Режим 2:** Отдельный режим "Написать пост"
  - Генерация поста только по теме
  - Полная структура поста: проблема → инсайт → тезисы → микропольза → CTA
- HTML-форматирование для Telegram
- Длина поста: ~1500 символов

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/iuriidurov/carousel-bot.git
cd carousel-bot
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
REPLICATE_API_KEY=your_replicate_api_key
KIE_API_KEY=your_kie_api_key
ADMIN_CHAT_ID=your_chat_id  # Опционально, для автоматической загрузки фоновых изображений
```

**Где получить:**
- `TELEGRAM_BOT_TOKEN`: Создайте бота через [@BotFather](https://t.me/BotFather)
- `REPLICATE_API_KEY`: Получите на [replicate.com](https://replicate.com)
- `KIE_API_KEY`: Получите на [kie.ai](https://kie.ai)
- `ADMIN_CHAT_ID`: Узнайте через [@userinfobot](https://t.me/userinfobot)

### 5. Подготовка фоновых изображений

Убедитесь, что в папке `background/` есть файл:
- `image2.jpg` — референс для слайдов 2-N (используется постоянно)

**Примечание:** `image1.jpg` больше не используется глобально. Изображение для первого слайда запрашивается у пользователя для каждой генерации.

### 6. Настройка доступа

Отредактируйте файл `app/handlers/user_handlers.py` и укажите список разрешенных пользователей:

```python
ALLOWED_USER_IDS = [649760082, 617934115]  # Замените на ваши Telegram ID
```

### 7. Загрузка фоновых изображений

**Вариант 1 (автоматический):**
- Задайте `ADMIN_CHAT_ID` в `.env`
- Изображение `image2.jpg` загрузится автоматически при старте бота
- URL сохранится в файл `app/background_urls.json`

**Вариант 2 (ручной):**
- Запустите бота
- Отправьте команду `/upload_backgrounds` в личные сообщения боту
- Бот загрузит изображение и сохранит его URL

## ▶️ Запуск

### Локальный запуск

**Windows:**
```bash
python main.py
```

**Или через run.bat:**
```bash
.\run.bat
```

### Запуск на сервере (systemd)

Создайте файл `/etc/systemd/system/carousel-bot.service`:

```ini
[Unit]
Description=Carousel Bot Telegram
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/projects/carousel_bot
Environment="PATH=/root/projects/carousel_bot/venv/bin"
ExecStart=/root/projects/carousel_bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl start carousel-bot
sudo systemctl enable carousel-bot
sudo systemctl status carousel-bot
```

Просмотр логов:
```bash
sudo journalctl -u carousel-bot -f
```

## 📖 Использование

### 📊 Режим "Карусель"

1. Нажмите кнопку **"📊 Карусель"** или отправьте текст "Карусель"
2. Отправьте тему для карусели текстовым сообщением
   - Пример: "Почему перфекционисты склонны к тревожности"
3. Бот попросит прислать изображение для первого слайда
   - 📸 Отправьте фотографию, которую хотите использовать
4. Бот попросит указать количество слайдов
   - 🔢 Напишите число от 2 до 20 (например: 5, 8, 10)
5. Бот автоматически:
   - Сгенерирует структуру контента через Gemini-3-PRO
   - Создаст слайды с изображениями через Nana Banana PRO
   - Отправит каждый слайд по мере готовности
6. После завершения бот спросит, хотите ли вы получить инфографику
   - Ответьте "да" или "нет"
7. После инфографики (или если отказались) бот спросит про пост
   - Ответьте "да" или "нет"

### 📈 Режим "Инфографика"

1. Нажмите кнопку **"📈 Инфографика"** или отправьте текст "Инфографика"
2. Отправьте тему для инфографики текстовым сообщением
   - Пример: "Как справиться с тревогой"
3. Бот автоматически:
   - Сгенерирует структуру через Gemini-3-PRO (JSON с заголовком и 4 советами)
   - Создаст инфографику через Nana Banana PRO
   - Отправит готовую инфографику

### 📝 Режим "Написать пост"

1. Нажмите кнопку **"📝 Написать пост"**
2. Отправьте тему для поста текстовым сообщением
   - Пример: "Почему мы боимся быть уязвимыми"
3. Бот автоматически:
   - Сгенерирует пост через Gemini-3-PRO
   - Создаст структурированный пост: проблема → инсайт → тезисы → микропольза → CTA
   - Отправит готовый пост с HTML-форматированием

### Команды

- `/start` — Начать работу с ботом
- `/help` — Справка по использованию
- `/upload_backgrounds` — Загрузить фоновое изображение image2 (только для разрешенных пользователей)

## 📁 Структура проекта

```
carousel_bot/
├── app/
│   ├── handlers/
│   │   └── user_handlers.py      # Обработчики команд и генерации
│   ├── services/
│   │   ├── gemini_service.py     # Сервис для Gemini-3-PRO
│   │   └── image_gen_service.py  # Сервис для Nana Banana PRO
│   ├── utils/
│   │   ├── prompts.py            # Системные промпты для всех режимов
│   │   └── background_utils.py   # Утилиты для работы с URL фонов
│   └── config.py                 # Конфигурация
├── background/
│   └── image2.jpg                 # Референс для слайдов 2-N
├── post_from_carousel.txt         # Промпт для поста из карусели
├── post_without_carousel.txt      # Промпт для поста без карусели
├── main.py                        # Точка входа
├── requirements.txt               # Зависимости
└── .env                           # Переменные окружения (создать вручную)
```

## 🔧 Технические детали

### Генерация карусели

1. **Слайд 1 (обложка):**
   - Использует изображение, отправленное пользователем
   - Затемненный фон (40-50%) для контраста
   - Белый/светлый текст с тенью для читаемости
   - Заголовок и подзаголовок

2. **Слайды 2-(N-1) (промежуточные):**
   - Используют `image2.jpg` как референс для фона
   - Светлый фон с легкой текстурой
   - Текст с буллитами
   - Минималистичные декоративные элементы

3. **Последний слайд (N):**
   - Использует `image2.jpg` как референс для фона
   - Вывод и CTA (Call to Action)

### Генерация инфографики

- **После карусели:** Прямая генерация через Nana Banana Pro (без Gemini)
- **Отдельный режим:** Gemini генерирует JSON структуру → Nana Banana Pro создает изображение
- Формат: 4:5, разрешение 2K
- Все тексты строго на русском языке

### Генерация постов

- **Из карусели:** Использует JSON структуру карусели для создания поста
- **Отдельный режим:** Генерирует пост только на основе темы
- Формат: HTML для Telegram (`parse_mode='HTML'`)
- Структура: Проблема → Инсайт → Тезисы → Микропольза → CTA
- Длина: ~1500 символов
- Автоматическая очистка от markdown и лишних символов

### Валидация URL

- Перед использованием URL проверяется:
  - Формат (должен начинаться с `http://` или `https://`)
  - Доступность (HTTP HEAD запрос, должен вернуть 200)
- Если URL недоступен, генерация происходит в режиме text-to-image (без референса)

### Обработка ошибок

- **Невалидный JSON от Gemini:**
  - Автоматическое исправление через `json_repair`
  - Повторный запрос (максимум 3 попытки)
- **Ошибка генерации изображения (422, таймаут и т.д.):**
  - Пропуск проблемного слайда
  - Продолжение генерации остальных
  - Повторная попытка для проблемных слайдов (максимум 2 попытки на слайд)
- **Недоступный URL фонового изображения:**
  - Автоматический переход в режим text-to-image
  - Логирование предупреждения

### Языковые требования

- Все промпты для генерации изображений содержат строгие требования:
  - **"ALL TEXT MUST BE STRICTLY IN RUSSIAN LANGUAGE ONLY"**
  - **"NO ENGLISH TEXT ALLOWED"**
- Это гарантирует, что инфографика и карусели содержат только русский текст

### Управление фоновыми изображениями

- **image1:** Запрашивается у пользователя для каждой генерации (не сохраняется)
- **image2:** Загружается один раз, сохраняется в `app/background_urls.json`, используется для всех слайдов 2-N
- **Автоматическая загрузка:** При старте бота проверяется наличие сохраненного URL, если его нет и задан `ADMIN_CHAT_ID`, выполняется автоматическая загрузка
- **Обновление URL:** Если URL истек (ошибки 422), используйте команду `/upload_backgrounds` для обновления

## 🛠️ Технологии

- **Python 3.10+**
- **python-telegram-bot 21.9** — работа с Telegram Bot API
- **Gemini-3-PRO** (через Replicate API) — генерация контента
- **Nana Banana PRO** (через Kie.ai API) — генерация изображений
- **json-repair** — исправление JSON от Gemini
- **httpx** — асинхронные HTTP запросы
- **loguru** — логирование
- **pydantic** — валидация конфигурации
- **python-dotenv** — загрузка переменных окружения

## 📝 Логирование

Бот использует `loguru` для логирования. Все важные события записываются в консоль:
- Инициализация сервисов
- Генерация контента и изображений
- Ошибки и предупреждения
- Валидация URL
- Статус задач генерации

## 🔐 Безопасность

- Система доступа по списку разрешенных пользователей
- Переменные окружения для чувствительных данных (токены, ключи)
- `.env` файл исключен из git (через `.gitignore`)
- Фоновые изображения не загружаются в репозиторий

## 📄 Лицензия

Проект создан для внутреннего использования.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все переменные окружения заданы правильно
3. Проверьте доступность API (Replicate, Kie.ai)
4. Убедитесь, что `image2.jpg` загружен через `/upload_backgrounds`

## 📌 Версия

Текущая версия включает:
- ✅ Генерация каруселей с динамическим image1
- ✅ Генерация инфографики (после карусели и отдельно)
- ✅ Генерация постов (из карусели и отдельно)
- ✅ HTML-форматирование постов
- ✅ Улучшенная обработка ошибок
