# Telegram-бот "Carousel Bot"

Бот для автоматической генерации Instagram-каруселей, инфографики и постов для психологического блога с использованием AI.

## 🚀 Возможности

### 📊 Генерация каруселей
- Создание каруселей из 2-20 слайдов (формат 4:5)
- Динамический запрос изображения для первого слайда у пользователя
- Генерация контента через **Gemini-3-PRO** (Replicate API)
- Генерация изображений через **Nana Banana PRO** (Kie.ai API)
- Затемненный фон на первом слайде для лучшей читаемости текста
- Последовательная отправка слайдов по мере готовности
- Автоматическое наложение водяного знака (логотип) на все слайды
- Автоматическое сохранение всех промптов и визуалов в Airtable

### 📈 Генерация инфографики
- **Режим 1:** После генерации карусели (опционально)
- **Режим 2:** Отдельный режим с генерацией структуры через Gemini
- Строгие требования к русскому языку
- Автоматическая отправка готовой инфографики
- Автоматическое сохранение промпта и визуала в Airtable

### 📝 Генерация постов
- **Режим 1:** Пост на основе карусели (после инфографики, опционально)
  - Использует структуру и контент из JSON карусели
  - Превращает тезисы в живой текст поста
- **Режим 2:** Отдельный режим "Написать пост"
  - Генерация поста только по теме
  - Полная структура поста: проблема → инсайт → тезисы → микропольза → CTA
- HTML-форматирование для Telegram
- Длина поста: ~1500 символов
- Автоматическое сохранение текста поста в Airtable

### 💾 Интеграция с Airtable
- Автоматическое сохранение всех данных после генерации карусели:
  - Тема от пользователя и дата запроса
  - Количество слайдов
  - Изображение для первого слайда (attachment)
  - Промпты для каждого слайда (Prompt_slide1 до Prompt_slide8)
  - Готовые визуалы слайдов (Visual_slide1 до Visual_slide8)
  - Промпт и визуал инфографики (если сгенерирована)
  - Текст поста (если сгенерирован)
- Автоматическое обновление записей при регенерации контента
- Удобное редактирование промптов и текстов прямо в таблице Airtable

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/iuriidurov/carousel-bot.git
cd carousel-bot
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
REPLICATE_API_KEY=your_replicate_api_key
KIE_API_KEY=your_kie_api_key
ADMIN_CHAT_ID=your_chat_id  # Опционально, для автоматической загрузки фоновых изображений
AIRTABLE_API_TOKEN=your_airtable_pat_token  # Опционально, для сохранения данных в Airtable
AIRTABLE_BASE_ID=your_airtable_base_id  # Опционально, ID базы Airtable
AIRTABLE_TABLE_ID=your_airtable_table_id  # Опционально, ID таблицы Airtable
```

**Где получить:**
- `TELEGRAM_BOT_TOKEN`: Создайте бота через [@BotFather](https://t.me/BotFather)
- `REPLICATE_API_KEY`: Получите на [replicate.com](https://replicate.com)
- `KIE_API_KEY`: Получите на [kie.ai](https://kie.ai)
- `ADMIN_CHAT_ID`: Узнайте через [@userinfobot](https://t.me/userinfobot)
- `AIRTABLE_API_TOKEN`: Создайте Personal Access Token (PAT) на [airtable.com](https://airtable.com/create/tokens)
- `AIRTABLE_BASE_ID`: Найдете в URL вашей базы Airtable (например: `appXXXXXXXXXXXXXX`)
- `AIRTABLE_TABLE_ID`: Найдете в URL вашей таблицы Airtable (например: `tblXXXXXXXXXXXXXX`)

### 5. Подготовка фоновых изображений и логотипа

Убедитесь, что в папке `background/` есть файлы:
- `image2.jpg` — референс для слайдов 2-N (используется постоянно)
- `logo.png` — логотип для водяного знака (PNG с прозрачностью, рекомендуется 3000x3000px)

**Примечание:** `image1.jpg` больше не используется глобально. Изображение для первого слайда запрашивается у пользователя для каждой генерации.

**Водяной знак:**
- Логотип автоматически накладывается на все слайды карусели (кроме последнего)
- На первом слайде: левый верхний угол, светлый вариант (для темного фона)
- На слайдах 2 до предпоследнего: правый нижний угол, обычный вариант
- На последнем слайде: без логотипа (чтобы не перекрывать CTA)
- На инфографике: без логотипа

### 6. Настройка доступа

Отредактируйте файл `app/handlers/user_handlers.py` и укажите список разрешенных пользователей:

```python
ALLOWED_USER_IDS = [99999991, 99999992]  # Замените на ваши Telegram ID
```

### 7. Загрузка фоновых изображений

**Вариант 1 (автоматический):**
- Задайте `ADMIN_CHAT_ID` в `.env`
- Изображение `image2.jpg` загрузится автоматически при старте бота
- URL сохранится в файл `app/background_urls.json`

**Вариант 2 (ручной):**
- Запустите бота
- Отправьте команду `/upload_backgrounds` в личные сообщения боту
- Бот загрузит изображение и сохранит его URL

## ▶️ Запуск

### Локальный запуск

**Windows:**
```bash
python main.py
```

**Или через run.bat:**
```bash
.\run.bat
```

### Запуск на сервере (systemd)

Создайте файл `/etc/systemd/system/carousel-bot.service`:

```ini
[Unit]
Description=Carousel Bot Telegram
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/projects/carousel_bot
Environment="PATH=/root/projects/carousel_bot/venv/bin"
ExecStart=/root/projects/carousel_bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl start carousel-bot
sudo systemctl enable carousel-bot
sudo systemctl status carousel-bot
```

Просмотр логов:
```bash
sudo journalctl -u carousel-bot -f
```

## 📖 Использование

### 📊 Режим "Карусель"

1. Нажмите кнопку **"📊 Карусель"** или отправьте текст "Карусель"
2. Отправьте тему для карусели текстовым сообщением
   - Пример: "Почему перфекционисты склонны к тревожности"
3. Бот попросит прислать изображение для первого слайда
   - 📸 Отправьте фотографию, которую хотите использовать
4. Бот попросит указать количество слайдов
   - 🔢 Напишите число от 2 до 20 (например: 5, 8, 10)
5. Бот автоматически:
   - Сгенерирует структуру контента через Gemini-3-PRO
   - Создаст слайды с изображениями через Nana Banana PRO
   - Отправит каждый слайд по мере готовности
6. После завершения бот спросит, хотите ли вы получить инфографику
   - Ответьте "да" или "нет"
7. После инфографики (или если отказались) бот спросит про пост
   - Ответьте "да" или "нет"
8. После генерации всех слайдов бот спросит, хотите ли переделать какой-то слайд
   - Ответьте "да" или "нет"
   - Если "да", укажите номер слайда (1, 2, 3...)
   - Бот попросит вас отредактировать промпт для этого слайда в таблице Airtable
   - Отредактируйте промпт в таблице (столбец Prompt_slideN) и напишите "+" в чат
   - Бот переделает слайд с новым промптом

### 📈 Режим "Инфографика"

1. Нажмите кнопку **"📈 Инфографика"** или отправьте текст "Инфографика"
2. Отправьте тему для инфографики текстовым сообщением
   - Пример: "Как справиться с тревогой"
3. Бот автоматически:
   - Сгенерирует структуру через Gemini-3-PRO (JSON с заголовком и 4 советами)
   - Создаст инфографику через Nana Banana PRO
   - Отправит готовую инфографику
4. После генерации бот спросит, хотите ли переделать инфографику
   - Ответьте "да" или "нет"
   - Если "да", бот попросит вас отредактировать промпт для генерации инфографики в таблице Airtable
   - Отредактируйте промпт в таблице (столбец Prompt_infografic) и напишите "+" в чат
   - Бот переделает инфографику с новым промптом


### 📝 Режим "Написать пост"

1. Нажмите кнопку **"📝 Написать пост"**
2. Отправьте тему для поста текстовым сообщением
   - Пример: "Почему мы боимся быть уязвимыми"
3. Бот автоматически:
   - Сгенерирует пост через Gemini-3-PRO
   - Создаст структурированный пост: проблема → инсайт → тезисы → микропольза → CTA
   - Отправит готовый пост с HTML-форматированием
4. После генерации бот спросит, хотите ли переделать пост
   - Ответьте "да" или "нет"
   - Если "да", бот попросит вас отредактировать текст поста в таблице Airtable
   - Отредактируйте текст в таблице (столбец Post_text) и напишите "+" в чат
   - Бот переделает пост с новым текстом

### Команды

- `/start` — Начать работу с ботом
- `/help` — Справка по использованию
- `/upload_backgrounds` — Загрузить фоновое изображение image2 (только для разрешенных пользователей)

## 📁 Структура проекта

```
carousel_bot/
├── app/                           # Основной пакет приложения
│   ├── __init__.py
│   ├── config.py                  # Конфигурация (настройки, переменные окружения)
│   ├── background_urls.json       # Сохраненные URL фоновых изображений
│   ├── handlers/                  # Обработчики команд и сообщений
│   │   ├── __init__.py
│   │   └── user_handlers.py       # Основные обработчики (команды, генерация)
│   ├── services/                  # Сервисы для работы с внешними API
│   │   ├── __init__.py
│   │   ├── gemini_service.py      # Сервис для Gemini-3-PRO (Replicate API)
│   │   ├── image_gen_service.py   # Сервис для Nana Banana PRO (Kie.ai API)
│   │   └── airtable_service.py   # Сервис для работы с Airtable API
│   └── utils/                     # Утилиты и промпты
│       ├── __init__.py
│       ├── prompts.py             # Системные промпты для всех режимов
│       ├── background_utils.py    # Утилиты для работы с URL фонов
│       └── watermark.py           # Утилиты для наложения водяного знака
├── background/                    # Фоновые изображения и логотип
│   ├── image2.jpg                  # Референс для слайдов 2-N (статический фон)
│   └── logo.png                    # Логотип для водяного знака
├── main.py                        # Точка входа приложения
├── requirements.txt               # Python зависимости
├── .env                           # Переменные окружения (создать вручную)
├── post_from_carousel.txt         # Промпт для поста из карусели (справочный)
├── post_without_carousel.txt      # Промпт для поста без карусели (справочный)
└── README.md                      # Документация
```

## 🔧 Технические детали

### Генерация карусели

1. **Слайд 1 (обложка):**
   - Использует изображение, отправленное пользователем
   - Затемненный фон (40-50%) для контраста
   - Белый/светлый текст с тенью для читаемости
   - Заголовок и подзаголовок
   - **Водяной знак:** Логотип в левом верхнем углу (светлый вариант для темного фона)

2. **Слайды 2-(N-1) (промежуточные):**
   - Используют `image2.jpg` как референс для фона
   - Светлый фон с легкой текстурой
   - Текст с буллитами
   - **Водяной знак:** Логотип в правом нижнем углу (обычный вариант)
   - Строгие запреты на добавление лишнего контента (username, социальные сети, водяные знаки)

3. **Последний слайд (N):**
   - Использует `image2.jpg` как референс для фона
   - Вывод и CTA (Call to Action)
   - **Без водяного знака** (чтобы не перекрывать CTA)
   - Строгие запреты на добавление любого текста, кроме предоставленного CTA

### Генерация инфографики

- **После карусели:** Прямая генерация через Nana Banana Pro (без Gemini)
- **Отдельный режим:** Gemini генерирует JSON структуру → Nana Banana Pro создает изображение
- Формат: 4:5, разрешение 2K
- Все тексты строго на русском языке

### Генерация постов

- **Из карусели:** Использует JSON структуру карусели для создания поста
- **Отдельный режим:** Генерирует пост только на основе темы
- Формат: HTML для Telegram (`parse_mode='HTML'`)
- Структура: Проблема → Инсайт → Тезисы → Микропольза → CTA
- Длина: ~1500 символов
- Автоматическая очистка от markdown и лишних символов

### Валидация URL

- Перед использованием URL проверяется:
  - Формат (должен начинаться с `http://` или `https://`)
  - Доступность (HTTP HEAD запрос, должен вернуть 200)
- Если URL недоступен, генерация происходит в режиме text-to-image (без референса)

### Обработка ошибок

- **Невалидный JSON от Gemini:**
  - Автоматическое исправление через `json_repair`
  - Повторный запрос (максимум 3 попытки)
- **Ошибка генерации изображения (422, таймаут и т.д.):**
  - Пропуск проблемного слайда
  - Продолжение генерации остальных
  - Повторная попытка для проблемных слайдов (максимум 2 попытки на слайд)
- **Недоступный URL фонового изображения:**
  - Автоматический переход в режим text-to-image
  - Логирование предупреждения

### Языковые требования и ограничения контента

- Все промпты для генерации изображений содержат строгие требования:
  - **"ALL TEXT MUST BE STRICTLY IN RUSSIAN LANGUAGE ONLY"**
  - **"NO ENGLISH TEXT ALLOWED"**
- Это гарантирует, что инфографика и карусели содержат только русский текст
- **Запреты на добавление лишнего контента:**
  - Запрещено добавлять username, Instagram handles, @ символы, адреса социальных сетей
  - Запрещено добавлять водяные знаки, подписи, брендинг
  - Запрещено добавлять текст, не предоставленный в промпте
  - Контент должен строго соответствовать предоставленному тексту в кавычках

### Управление фоновыми изображениями

- **image1:** Запрашивается у пользователя для каждой генерации (не сохраняется)
- **image2:** Загружается один раз, сохраняется в `app/background_urls.json`, используется для всех слайдов 2-N
- **Автоматическая загрузка:** При старте бота проверяется наличие сохраненного URL, если его нет и задан `ADMIN_CHAT_ID`, выполняется автоматическая загрузка
- **Обновление URL:** Если URL истек (ошибки 422), используйте команду `/upload_backgrounds` для обновления

## 🛠️ Технологии

- **Python 3.10+**
- **python-telegram-bot 21.9** — работа с Telegram Bot API
- **Gemini-3-PRO** (через Replicate API) — генерация контента
- **Nana Banana PRO** (через Kie.ai API) — генерация изображений
- **Airtable API** (через pyairtable) — сохранение и управление данными
- **Pillow (PIL)** — обработка изображений и наложение водяного знака
- **json-repair** — исправление JSON от Gemini
- **httpx** — асинхронные HTTP запросы
- **loguru** — логирование
- **pydantic** — валидация конфигурации
- **python-dotenv** — загрузка переменных окружения

## 📝 Логирование

Бот использует `loguru` для логирования. Все важные события записываются в консоль:
- Инициализация сервисов
- Генерация контента и изображений
- Ошибки и предупреждения
- Валидация URL
- Статус задач генерации

## 🔐 Безопасность

- Система доступа по списку разрешенных пользователей
- Переменные окружения для чувствительных данных (токены, ключи)
- `.env` файл исключен из git (через `.gitignore`)
- Фоновые изображения не загружаются в репозиторий

## 📄 Лицензия

Проект создан для внутреннего использования.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все переменные окружения заданы правильно
3. Проверьте доступность API (Replicate, Kie.ai)
4. Убедитесь, что `image2.jpg` загружен через `/upload_backgrounds`

## 📊 Интеграция с Airtable

Бот может автоматически сохранять все данные в Airtable (опционально):

### Что сохраняется:
- **Тема от пользователя** — исходная тема запроса
- **Дата запроса** — дата создания записи
- **Количество слайдов** — количество слайдов в карусели
- **Изображение для первого слайда** — attachment из Telegram
- **Промпты для каждого слайда** (Prompt_slide1 до Prompt_slide8) — полные промпты для Nana Banana
- **Визуалы слайдов** (Visual_slide1 до Visual_slide8) — готовые изображения
- **Промпт инфографики** (Prompt_infografic) — промпт для генерации инфографики
- **Визуал инфографики** (Visual_infografic) — готовое изображение инфографики
- **Текст поста** (Post_text) — готовый текст поста

### Регенерация через Airtable:
- После генерации карусели создается запись в Airtable
- Пользователь может отредактировать промпт/текст прямо в таблице
- После редактирования пользователь пишет "+" в чат
- Бот читает обновленный промпт/текст из Airtable и регенерирует контент
- Новый визуал/текст автоматически обновляется в Airtable

### Настройка Airtable:
1. Создайте базу данных в Airtable
2. Создайте таблицу с необходимыми полями (см. список выше)
3. Создайте Personal Access Token (PAT) в настройках Airtable
4. Добавьте токен и ID базы/таблицы в `.env`

## 🎨 Водяной знак (логотип)

Бот автоматически накладывает логотип на все слайды карусели:

### Позиционирование:
- **Первый слайд:** Левый верхний угол, светлый вариант (для темного фона)
- **Слайды 2 до предпоследнего:** Правый нижний угол, обычный вариант
- **Последний слайд:** Без логотипа (чтобы не перекрывать CTA)
- **Инфографика:** Без логотипа

### Технические детали:
- Логотип автоматически масштабируется до 20% ширины слайда
- Прозрачность: 80%
- Отступ от края: 5%
- Обработка выполняется через Pillow (PIL)
- В случае ошибки обработки отправляется оригинальное изображение

## 🔄 Регенерация контента

Бот поддерживает регенерацию слайдов, инфографики и постов:

### Регенерация слайдов карусели:
1. После генерации всех слайдов бот спрашивает о регенерации
2. Пользователь выбирает номер слайда для переделки
3. Бот отправляет промпт для редактирования в Airtable
4. Пользователь редактирует промпт в таблице и пишет "+"
5. Бот читает обновленный промпт и регенерирует слайд
6. Новое изображение автоматически обновляется в Airtable

### Регенерация инфографики:
- **В режиме карусели:** Через Airtable (аналогично слайдам)
- **В standalone режиме:** Прямой ввод промпта (бот отправляет промпт для редактирования, пользователь отправляет новый)

### Регенерация поста:
1. После генерации поста бот спрашивает о регенерации
2. Бот отправляет текст для редактирования в Airtable
3. Пользователь редактирует текст в таблице и пишет "+"
4. Бот читает обновленный текст и регенерирует пост
5. Новый текст автоматически обновляется в Airtable

## 📌 Версия

Текущая версия включает:
- ✅ Генерация каруселей с динамическим image1
- ✅ Генерация инфографики (после карусели и отдельно)
- ✅ Генерация постов (из карусели и отдельно)
- ✅ HTML-форматирование постов
- ✅ Улучшенная обработка ошибок
- ✅ Водяной знак (логотип) на слайдах через Pillow
- ✅ Интеграция с Airtable для сохранения данных
- ✅ Регенерация слайдов, инфографики и постов
- ✅ Улучшенные промпты с запретами на лишний контент
- ✅ Расширенное логирование всех операций
